# Previous Dev (archived)

Application code now lives at the repository root:

- `contracts/`, `test/`, `scripts/` — Hardhat smart-contract toolchain
- `backend/` — Express API
- `frontend/` — React DApp
- `ml-service/` — FastAPI scoring service

See the root README.md for setup.
